<div class="comment-form-container">
  <h3 class="comment-heading">Create a new comment 💬</h3>

  <%= form_with model: [@user, @post, @comment], url: user_post_comments_path, method: :post, local: true, scope: :comment, class: 'comment-form' do |form| %>
    <div class="form-group">
      <%= form.label :text, class: 'form-label' %><br>
      <%= form.text_field :text, class: 'form-control' %>
    </div>
    <%= form.submit "Add Comment", class: "submit-comment" %>
  <% end %>
</div>
